#include "main.h"
/**
 * _puts - Prints a string, followed by a new line, to stdout
 *
 * @str: The pointer to the first character in the string
 *
 * Return: void
 */
void _puts(char *str)
{
	while (*str != '\0')
	{
		str++;
	}
}
